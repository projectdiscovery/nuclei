id: excluded-template

info:
  name: Basic Excluded Template
  author: pdteam
  severity: info
  tags: fuzz

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    matchers:
      - type: word
        words:
          - "This is test matcher text"