id: basic-get-sni

info:
  name: Basic GET Request with CLI SNI
  author: pdteam
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    matchers:
      - type: word
        words:
          - "test-ok"