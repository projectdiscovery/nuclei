id: get-all-ips

info:
  name: Basic GET Request on all IPS
  author: pdteam
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    matchers:
      - type: word
        words:
          - "ok"