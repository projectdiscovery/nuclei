id: test-javascript-ssh
info:
  name: test-javascript-ssh
  author: Ice3man
  severity: high

javascript:
  - code: >
      var m = require("nuclei/ssh");
      var c = m.Client();
      c.Connect(Host, 22, "root", "HbvTty!c!VQ!3nJT")
    args:
      Host: "{{Host}}"
    output: false
    
    matchers:
      - type: word
        part: response
        words:
          - "true"