name: Typos Check

on:
  push:
    branches: ["dev"]
  pull_request:
  workflow_dispatch:

jobs:
  typos:
    name: Spell Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: crate-ci/typos@v1.43.0
