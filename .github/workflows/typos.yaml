name: Typos Check

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  typos:
    name: Check for Typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for typos
        uses: crate-ci/typos@master
