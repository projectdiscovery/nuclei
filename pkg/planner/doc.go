// Package planner plans and creates a scan query graph schema.
//
// The created schema from the planner is then used for scan execution./
// Exact requests are clustered together into one from all templates.
package planner
