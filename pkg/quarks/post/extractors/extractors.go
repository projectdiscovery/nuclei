package extractors

// Extractor is a extractor structure parsed from a yaml file
type Extractor struct{}

// CompiledExtractor is the compiled extractor parsed from yaml file.
type CompiledExtractor struct {
	Internal bool
}
