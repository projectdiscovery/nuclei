id: variables-example

info:
  name: Variables Example
  author: pdteam
  severity: info

variables:
  a1: "FQDN"
  a2: "IN"

dns:
  - name: "{{a1}}"
    type: A
    class: inet
    recursion: true
    retries: 3
    matchers:
      - type: word
        words:
          - "{{a2}}"