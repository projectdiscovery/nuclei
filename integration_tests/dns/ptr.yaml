id: ptr-fingerprint

info:
  name: PTR Fingerprint
  author: pdteam
  severity: info
  tags: dns,ptr

dns:
  - name: "{{FQDN}}"
    type: PTR

    matchers:
      - type: regex
        regex:
          - "IN\tPTR"
          - "IN\\s*PTR"

    extractors:
      - type: regex
        group: 1
        regex:
          - "IN\tPTR\t(.+)"